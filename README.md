# Задание 1: Юнит-тесты

Написаны юнит-тесты для класса `Burger` (Stellar Burgers).
Подключены `pytest` и `pytest-cov`. Покрытие `praktikum/burger.py` — 100%.

## Установка
```bash
pip install -r requirements.txt
